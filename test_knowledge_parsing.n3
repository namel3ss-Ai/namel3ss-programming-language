app "family_tree".

knowledge family:
    fact parent(alice, bob).
    fact parent(bob, charlie).
    rule ancestor(X, Y) :- parent(X, Y).
    rule ancestor(X, Z) :- parent(X, Y), ancestor(Y, Z).

query find_ancestors:
    from knowledge family:
    goal ancestor(alice, Who)
    variables: Who
