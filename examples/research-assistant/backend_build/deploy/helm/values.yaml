replicaCount: 2

  image:
repository: ghcr.io/your-org/research-assistant
    tag: "latest"
    pullPolicy: IfNotPresent

  service:
    type: ClusterIP
    port: 80

  ingress:
    enabled: false
    className: ""
    hosts:
      - host: example.com
        paths:
          - path: /
            pathType: Prefix
    tls: []

  env:
    NAMEL3SS_ENV: production
